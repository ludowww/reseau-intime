{
  "version": "0.9",
  "modes": {
    "source_memory_coverage_check": {
      "purpose": "Verify that a source scene creates the memory traces promised by its memory contract.",
      "required": [
        "matching_source_scene",
        "memory_out_present",
        "route_consistency",
        "core_memory_traces_covered"
      ]
    },
    "future_continuity_check": {
      "purpose": "Verify that a later scene remembers or respects a previous memory contract.",
      "required": [
        "target_scene_present",
        "route_consistency",
        "at_least_one_callback_or_trace",
        "no_forbidden_reset"
      ]
    }
  }
}
